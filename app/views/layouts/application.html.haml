!!!
%html
  %head
    %title= h(yield(:title) || "Untitled")
    = javascript_include_tag 'jquery-1.4.1', 'application', 'farbtastic'
    = stylesheet_link_tag 'application', 'farbtastic', 'formtastic', 'formtastic_changes'
    = yield :head
  %body
    %div{:id => "container"}
      - flash.each do |name, msg| 
        = content_tag :div, msg, :id => "flash_#{name}"
      - if show_title?
        %h1=h yield :title
      = yield